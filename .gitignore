# Secrets — never commit. Copy .env.example to .env and fill it in locally.
.env
.env.*
!.env.example
*.key
*.pem

# Local backend data (LocalMemory JSONL files, etc.)
.data

# Dependencies + OS cruft
node_modules/
.DS_Store
npm-debug.log*

# foundry
lib/forge-std/
out/
cache/
